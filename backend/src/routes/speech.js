// ⚠️ DEPRECATED — Transcrição Whisper é feita no app mobile. Use POST /practice
// Este arquivo não é mais importado pelo app.js v2.
const express = require('express');
const router  = express.Router();
module.exports = router;
